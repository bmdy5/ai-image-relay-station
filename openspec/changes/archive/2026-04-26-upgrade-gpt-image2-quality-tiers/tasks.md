## 1. 后端参数分发器实现

- [ ] 1.1 在 `backend/api/image.py` 中定义 `TIER_CONFIG` 映射表，包含不同档次的分辨率和质量参数。
- [ ] 1.2 修改 `process_image_task` 函数，使其在构建 API 请求 Payload 时，根据传入的 `quality` 从映射表中取值。
- [ ] 1.3 为 `Master` 档次增加逻辑，如果选择该档次，则在 Payload 中添加 `quality: "hd"` 参数。
- [ ] 1.4 重启后端服务并使用 cURL 或脚本测试不同档次的 API 请求是否携带了预期参数。

## 2. 前端 UI 交互解锁

- [ ] 2.1 修改 `frontend/src/pages/HomePage.jsx`，移除 `高清版` 和 `大师版` 的 `disabled` 属性及置灰样式。
- [ ] 2.2 移除 `编辑图片` 切换按钮的禁用状态。
- [ ] 2.3 移除结果展示区的 `🚀 继续编辑` 按钮的禁用状态。
- [ ] 2.4 在规格按钮下方增加简短的优势说明（如“高清版：纵向超清适配”）。

## 3. 全链路验证

- [ ] 3.1 模拟用户生成一张“标准版”图片，检查后端日志确认 `size` 为 `1024x1024`。
- [ ] 3.2 模拟用户生成一张“大师版”图片，检查后端日志确认 `size` 为 `1024x1792` 且包含 `hd` 参数。
- [ ] 3.3 确认积分扣除额度与前端显示（5/10/15）完全匹配。
