## 1. 环境准备

- [x] 1.1 安装 `lucide-react` 依赖项：`npm install lucide-react`。

## 2. 首页 (HomePage) 视觉刷新

- [x] 2.1 替换顶部导航栏 Emoji：我的创作 (`Image`), 价格 (`Coins`), 管理后台 (`ShieldCheck`), 使用指南 (`BookOpen`)。
- [x] 2.2 优化参数面板：将“描述您的创意”前的 Emoji 替换为 `Sparkles`。
- [x] 2.3 升级生图按钮：将 `🚀` 替换为 `Zap` 或 `Wand2` 线性图标。
- [x] 2.4 更新结果展示区：将空状态的 `🎨` 替换为大尺寸的 `Palette` 图标。

## 3. 历史页 (HistoryPage) 视觉刷新

- [x] 3.1 优化搜索组件：将搜索框内的 `🔍` 和 `✕` 替换为 Lucide 的 `Search` 和 `X` 图标。
- [x] 3.2 替换卡片操作项：将“复用”按钮内的 Emoji 替换为 `RefreshCw` 或 `CopyPlus`。
- [x] 3.3 升级预览模态框：替换下载 (`Download`), 复用 (`RotateCcw`), 复制 (`ClipboardCopy`) 图标。

## 4. 全局收尾与自检

- [x] 4.1 检查全站图标的 `strokeWidth` 是否统一为 `1.75`。
- [x] 4.2 验证悬停 (Hover) 状态下图标与文字的颜色同步变化逻辑。
- [x] 4.3 主动复查：使用技能确保代码修改点精准，无冗余重构。
