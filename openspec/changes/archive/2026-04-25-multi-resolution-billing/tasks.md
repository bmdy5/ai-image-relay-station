## 1. 计费矩阵与后端基础

- [x] 1.1 修改 `backend/api/image.py`：更新 `PRICING` 常量为 `{standard: 5, hd: 15, master: 30}`。
- [x] 1.2 更新 `backend/schemas/image.py`：确保 `quality` 字段支持新档位名称。

## 2. 后端逻辑加固 (超时与并发)

- [x] 2.1 实现 60s 硬超时：在 `generate_image` 的 API 调用部分加入超时熔断。
- [x] 2.2 完善任务释放逻辑：确保在超时、报错、成功后都能正确释放并发槽位。
- [x] 2.3 开发“任务重置”接口：在 `user.py` 或 `admin.py` 中增加重置用户 `pending` 状态的 API。

## 3. 前端 UI 精简与升级

- [x] 3.1 `HomePage` 改造：移除分辨率选择，改为三个简洁的画质档位按钮（5/15/30）。
- [x] 3.2 `PricingPage` 改造：更新价格卡片描述，展示 3 档位新规则。
- [x] 3.3 `ProfilePage` 增强：在账户详情下方增加“任务重置/清理”按钮。
- [x] 3.4 交互反馈：在点击生成时显示“预计消耗 X 积分”，并在余额不足时引导。

## 4. 验证与审计

- [x] 4.1 端到端测试：模拟 1024px 下三种档位的扣费。
- [x] 4.2 假死模拟：人为制造 60s 延迟，验证系统是否自动解锁并退费。
- [x] 4.3 运行 `code_compliance_check`：审计代码逻辑一致性。
