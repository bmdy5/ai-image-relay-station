## ADDED Requirements

### Requirement: 用户注册限额
系统必须严格限制注册用户总数不超过 100 人。

#### Scenario: 注册名额已满
- **WHEN** 数据库中用户总数已达到 100 且新用户尝试注册
- **THEN** 系统必须拒绝注册请求并返回“内测名额已满”错误

### Requirement: 账号密码注册与登录
系统必须支持基于用户名和密码的身份验证，并使用加密存储。

#### Scenario: 成功登录并获取令牌
- **WHEN** 用户输入正确的账号和密码
- **THEN** 系统必须返回一个有效的 JWT 访问令牌，有效期为 24 小时
