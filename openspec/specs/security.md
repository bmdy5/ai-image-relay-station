# 安全规范文档 (Security Spec)

## 1. 密钥安全 (API Key Safety)
*   **存储位置**：所有外部 API Key（OpenAI, JWT Secret）必须存储在服务器环境变量中（Vercel Environment Variables）。
*   **前端隔离**：前端代码严禁包含任何 Key，仅通过后端 API 进行间接调用。
*   **日志过滤**：后端日志记录时，必须对返回的图片链接进行有效期控制（如果可能），且严禁在日志中记录用户的敏感参数。

## 2. 身份认证与权限 (Auth & Permissions)
*   **密码加密**：用户密码在存入数据库前必须经过 `bcrypt` 算法进行哈希，Work Factor 设置为 10 以上。
*   **会话管理**：使用 JWT (JSON Web Token) 进行无状态认证。Token 过期时间建议设置为 24 小时。
*   **管理员保护**：
    *   **路由守卫**：前端 Router 必须实现守卫钩子，校验 `is_admin` 权限，防止未授权访问管理员页面。
    *   **二次校验**：`/admin` 相关接口不仅校验 JWT，还必须额外校验管理专用密码（Header 传参）。

## 3. 防滥用与风控 (Anti-Abuse)
*   **注册限额 (Quota Control)**：
    *   全站用户总数上限：**100 人**。
    *   实现方式：在注册业务逻辑中，每次注册前执行 `SELECT COUNT(*) FROM users`，达到上限即熔断。
*   **单用户频率限制 (Rate Limiting)**：
    *   单 IP 每分钟最多发起 5 次 API 请求。
    *   单用户每小时最多发起 20 次生图请求。
*   **消费熔断**：
    *   在后端设置单日总积分消耗/API 成本上限。
    *   一旦今日 API 支出达到预设金额（如 $50），系统自动切换至“维护模式”。

## 4. 数据安全
*   **数据库备份**：定期导出 SQLite 文件进行异地备份。
*   **传输加密**：全站必须强制启用 HTTPS（Vercel 默认提供）。
