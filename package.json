{
  "name": "gpt-image2-relay-station",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../public && cp -r dist/* ../public/"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
